<div class="cards">
  <ul class="card unlist" style="--max:{{cardNumber}}">
    {{#times cardNumber}}
      <li style="--count:{{this}}">
        <img src="{{../icon}}">
      </li>
    {{/times}}
  </ul>
</div>