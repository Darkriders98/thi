<legend>{{label}}</legend>
<ul class="items">
  {{#if @root.editable}}
    <li class="item-header">
      <a class="item-control item-create" title="{{localize 'DOCUMENT.Create' type=label}}" data-action="createDoc"
        data-document-class="Item" data-type="magicalprof">
        <i class="fas fa-plus"></i>
        {{localize 'DOCUMENT.New' type=label}}
      </a>
    </li>
  {{/if}}
  <li class="item-header">
    <span>{{localize 'THI.Magic.Proficiency.FIELDS.xp.label'}}</span>
    <span>{{localize 'THI.Magic.Proficiency.FIELDS.mastery.label'}}</span>
    <span>{{localize 'THI.Magic.Proficiency.FIELDS.summary.label'}}</span>
  </li>
  {{#each items as |item|}}
    <li class="item flexrow draggable" data-item-id="{{item._id}}" data-document-class="Item">
      <img class="xp" width="80" height="80" data-fill="{{item.system.xp}}">
      <div class="stack-string">
        <span>{{localize 'THI.Magic.Proficiency.Mastery.Adept'}}</span>
        <span>{{localize 'THI.Magic.Proficiency.Mastery.Master'}}</span>
      </div>
      {{#times 3}}
        <span class="mastery {{#if (gt ../system.mastery this)}}filled{{/if}}"></span>
      {{/times}}
      <div>{{item.name}}</div>
      <div class="document-controls">
        <a class="item-control item-expand" title="{{localize 'DOCUMENT.Expand' type=item.name}}" data-action="expandDoc">
          <i class="{{localize 'ICONS.Description'}}"></i>
        </a>
        {{#if @root.editable}}
          <a class="item-control item-edit" title="{{localize 'DOCUMENT.Update' type=item.name}}" data-action="viewDoc">
            <i class="{{localize 'ICONS.Edit'}}"></i>
          </a>
          <a class="item-control item-delete" title="{{localize 'DOCUMENT.Delete' type=item.name}}" data-action="deleteDoc">
            <i class="{{localize 'ICONS.Delete'}}"></i>
          </a>
        {{/if}}
      </div>
    </li>
  {{/each}}
</ul>